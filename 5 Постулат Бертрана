'''
Постулат Бертрана (теорема Бертрана-Чебышева, теорема Чебышева) гласит, что для любого n > 1 найдется простое число p в интервале n < p < 2n.
Задача состоит в том, чтобы решить несколько более общую задачу – а именно по числу n найти количество простых чисел p из интервала n < p < 2n
'''

n = int(input())
count = 0
# функция считает все делители числа, если делителей более 2 => число не является простым
def is_plain(p):
    i = 1
    a = []
    while i*i <= p:
        if p % i == 0:
            a.append(i)
            if i != p // i:
                a.append(n // i)
        i += 1
    if len(a) > 2:
        return False
    else:
        return True

for i in range(n + 1, n * 2):
    if i % 2 == 0 and i != 2 or i == 1:
        continue
    if is_plain(i):
        count += 1
        
print(count)
