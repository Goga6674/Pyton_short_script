"""
На вход программе поступает положительное целое число n, а задача вывести в порядке возрастания все цифры, которые встречались в этом числе, и напротив каждого также необходимо вывести сколько раз данная цифра встречалась в числе n
"""
n = [int(i) for i in input()]      # читаем число и преобразуем его в список цифр образующих число
count = [0] * 10                   # создаём пустой список инициализируем 10-ю нулевыми ячейками (по числу цифр)
for i in n:                        # проходом по списку увеличиваем на 1 каждое вхождение цифры номинал которой равен индексу в списке-счетчике
    count[i] += 1
for i in range(len(count)):
    if count[i] > 0:
        print(i, count[i])
